# 贡献指南

感谢你对「Pi Agent 原理与实现」教程的兴趣！以下是参与贡献的指南。

---

## 如何贡献

### 报告问题

如果你发现了错误（错别字、代码 bug、概念误解、链接失效等），请通过以下方式报告：

1. **提交 Issue** — 描述问题所在页面、预期行为、实际行为
2. **直接提 PR** — 对于简单的修复（如错别字），直接修改并提交 PR 更高效

Issue 模板建议：

```markdown
**页面**: guide/05-agent-loop.md
**问题类型**: 概念错误 / 代码错误 / 错别字 / 链接失效 / 其他
**描述**: ...
**建议修复**: ...
```

### 改进内容

以下类型的贡献非常欢迎：

- **修正错误** — 技术概念、代码示例、链接等
- **补充说明** — 某个知识点讲得不清楚，可以补充解释或示例
- **增加练习** — 为某章添加小练习或思考题
- **优化图表** — 改进 Mermaid 图的可读性
- **翻译** — 将教程翻译成其他语言

### 添加新章节或 Demo

如果你想添加新的教学内容：

1. 先开 Issue 讨论大纲，避免与现有内容重复或冲突
2. 遵循现有的写作风格（工程师视角、讲清设计意图、含代码示例）
3. 新 Demo 需要可独立运行，附运行说明

---

## 写作规范

### 文件组织

- 原理篇放在 `docs/guide/`
- Demo 篇放在 `docs/demos/`
- 项目篇放在 `docs/project/`
- 每篇都需要在 `docs/.vitepress/config.mts` 的 sidebar 中注册

### Markdown 规范

- 使用中文撰写，技术术语保留英文（如 Agent、LLM、Event Stream）
- 代码块标注语言（```ts、```bash、```mermaid）
- 章节标题使用 `#` 到 `####` 四级
- 关键概念首次出现时加粗

### 代码示例规范

- TypeScript 优先，使用现代语法（ES2022+）
- 示例代码应该可以直接运行，或注明需要补充的部分
- 复杂示例添加行内注释说明关键逻辑

### Mermaid 图表

- 优先使用 `graph TD/LR`、`sequenceDiagram`、`stateDiagram-v2`
- 图表宽度控制在 80 字符以内，避免移动端溢出
- 复杂图表添加文字说明辅助理解

---

## 本地开发

```bash
# 1. Fork 并克隆仓库
git clone https://github.com/YOUR_NAME/pi-agent-tutorial.git
cd pi-agent-tutorial

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run docs:dev

# 4. 修改内容，浏览器实时刷新

# 5. 构建验证
npm run docs:build
```

---

## 提交规范

### Commit Message

```
<类型>: <简短描述>

<详细说明（可选）>
```

类型：

- `fix` — 修复错误
- `docs` — 文档修改
- `demo` — Demo 相关
- `project` — 项目代码相关
- `style` — 格式调整（不影响内容）
- `chore` — 构建/工具链调整

示例：

```
fix: 修正 agent-loop 时序图中的事件顺序

tool_execution_end 应该在 turn_end 之前触发，
原图顺序有误，已调整。
```

### PR 规范

1. PR 标题简明扼要说明修改内容
2. 描述中说明修改动机和主要变更
3. 确保 `npm run docs:build` 能成功构建
4. 如涉及代码变更，提供测试步骤

---

## 行为准则

- 保持友善和尊重，对新手友好
- 讨论聚焦技术和内容，避免人身攻击
- 接受不同观点，通过建设性讨论达成共识

---

如有疑问，欢迎开 Issue 讨论！
